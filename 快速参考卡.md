# 快速参考卡

> 适用版本: v3 · 最后更新: 2026-07-07 · 维护者: Yht20927

> 命令速查。**安全规则**见 [`全局规则.md`](./全局规则.md)；**执行函数**见 [`执行模板.md`](./执行模板.md)。

## 启动前

```bash
bash ~/.claude/skills/douyin-cli/scripts/bridge.sh ensure   # Bridge Server
node cli.js status                                            # 油猴连接
node cli.js preflight                                         # 节奏守卫状态
node cli.js replied > /tmp/replied_cids.txt                   # 已回复集合
```

## 核心命令

| 命令 | 用途 |
|------|------|
| `node cli.js my [--count N]` | 我的作品 |
| `node cli.js user <sec_uid\|URL>` | 用户作品 |
| `node cli.js search "kw" --count N` | 搜索视频 |
| `node cli.js get <id> --new --depth 1` | 增量获取评论（优先于 --all） |
| `node cli.js post <id> "文本" [--reply-to <cid>]` | 发表/回复评论 |
| `node cli.js like <id> [--unlike]` | 点赞/取消点赞 |
| `node cli.js delete-comment <cid>` | 删除评论 |
| `node cli.js download <id> [--audio-only]` | 下载视频/音频 |
| `node cli.js analyze <id>` | LLM 分析评论 |
| `node cli.js suggest <id> [--auto]` | LLM 回复建议 |
| `node cli.js browse --max-notes 2` | 模拟浏览行为 |
| `node cli.js dashboard` | 运营仪表盘 |

## 节奏与事实守卫

| 命令 | 用途 |
|------|------|
| `node cli.js preflight [cmd]` | 节奏自检（距下次写需等多久 / 命令是否可执行） |
| `node cli.js repo-info [<owner/repo>]` | GitHub 仓库真实信息（推广评论事实源，缓存 1h） |
| `node cli.js factcheck "<文本>"` | 推广评论事实校验（star/仓库名/版本号 vs repo-info 缓存） |
| `node cli.js dedup "<文本>"` | 查重（是否发过） |
| `node cli.js replied [--count]` | 已回复 cid 列表 |

## 记忆层（SQLite）

| 命令 | 用途 |
|------|------|
| `node cli.js whois <uid>` | 用户全量画像 |
| `node cli.js note <uid> [--tier\|--tag\|--notes]` | 标记用户 |
| `node cli.js corpus <search\|recent\|stats>` | 回复语料库 |
| `node cli.js failures [--limit N]` | 失败模式 top-N |
| `node cli.js log [--tail N] [--video <id>]` | 操作日志 |
| `node cli.js events` | 原始事件流 |
| `node cli.js cleanup [--days 90]` | 清理过期记忆 |

## 硬约束（速记）

- 写 40-55s / 读 30-50s 随机间隔（`risk-control` 硬强制 + `execute_with_delay` 补充）
- 同 cid 一生一次（SQL `replied=1`）；同作者 ≥7 天冷却
- 单轮 ≤10 条，每日 ≤3 轮
- 推广评论必含真实仓库信息（`repo-info`），发布前 `factcheck`
- 命令串行，禁并发
